# Legrand GeoAI — Lightweight dev requirements (sans OCR lourd)
# Usage: uv pip install -r requirements-dev.txt

# --- Web Framework ---
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-multipart>=0.0.12

# --- Database ---
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
alembic>=1.14.0
greenlet>=3.1.0

# --- Authentication ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.2.0

# --- Task Queue ---
arq>=0.26.0
redis>=5.2.0

# --- AI / RAG (léger) ---
chromadb>=0.5.0
httpx>=0.27.0
# sentence-transformers skipped for speed (reranker won't work)

# --- Document Processing ---
pymupdf>=1.24.0

# --- OCR (skipped for local dev — heavy deps) ---
# surya-ocr>=0.8.0
# paddleocr>=2.8.0
# paddlepaddle>=2.6.0

# --- Logging ---
structlog>=24.4.0

# --- Utils ---
pillow>=10.4.0
aiofiles>=24.1.0
python-magic>=0.4.27

# --- Auth schemas need this ---
email-validator>=2.0.0
